services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres-servicios
    hostname: postgres-servicios
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - ./data:/var/lib/postgresql/data
    restart: always
    networks:
      - red-servicios

networks:
  red-servicios:
    external: true
